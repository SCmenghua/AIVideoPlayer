      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project堟      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project湰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project凤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐢      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectㄤ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project簬      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project囦      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project欢      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍚      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嶃      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project佸      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project伐      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project浠      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project跺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project悕      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectq      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project0      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectB      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project&      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project5      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project4      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project0      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project@      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project5      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project*      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project2      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project[      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectz      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project]      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project;      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project湪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎵      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project惧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project埌      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectq      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project'      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project'      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project缂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project哄      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project皯      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectK      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project&      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project&      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectF      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project&      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project&      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectX      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectW      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project@      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project4      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectF      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project'      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project'      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project,      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project'      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project'      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectB      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectj      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectL      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectL      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectW      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectB      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project*      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project[      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectz      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project]      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project;      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project壘      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍒      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project版      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瀯      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project寤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project轰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project骇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectx      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectz      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectq      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectU      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectB      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectU      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project@      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project4      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectC      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectH      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectK      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectk      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project:      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project|      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project=      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectL      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project<      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project<      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectF      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project#      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project#      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project堟      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project湭      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project绛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project惧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project悕      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project渚      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涜      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嚜      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project绛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project惧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project伐      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍏      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project蜂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project娇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐢      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project細      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐖      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project辨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project濆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project姪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎵      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project绛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project夛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紝      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瀵      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project煎      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project叆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍚      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project庣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project敤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鑷      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project繁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectD      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project绛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project惧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project悕      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瀹      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project夎      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project銆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project闇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瑕      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project2      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project6      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project婁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project互      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project婅      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project澶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project囥      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project2      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project歎      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project￠      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project】      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project闂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏍      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project规      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project湰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project淇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鈥      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project斺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project旀      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project壒      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project閲      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project忔      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project洿      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project+      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鑺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project傛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project祦      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project哄      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project埗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project孲      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project寮      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project曞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project叆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project缂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project撳      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project啿      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍖      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project猴      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project夛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紝      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project蹇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瘡      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project5      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project0      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎵      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project归      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project噺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎻      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project愪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project氦      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project娆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project★      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紝      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project澶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectу      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project箙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project忓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project皯      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectU      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍒      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锋      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project柊      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project棰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project戠      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project巼      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project銆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project氳      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project繕      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍘      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project熷      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瓧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project骞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project曞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project啓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍏      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectヤ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project负      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍚      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project屾      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏂      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project规      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project硶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project屼      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project慨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project澶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project0      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project寮      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project曞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project叆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectU      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project′      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project綇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project銆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project佸      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瓧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project骞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project曞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project拰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project缈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project昏      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瘧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嶆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project樉      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project绀      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project洪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project棶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project棰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project樸      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project氳      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瘑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍒      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project彲      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐢      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectㄦ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectт      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project慨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project澶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嶁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project斺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project旀      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project爣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project+      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project█      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project浼      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project犻      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project掞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project枃      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嗗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project埆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project寸      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectǔ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project夈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嗗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project埆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project娓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project告      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project爣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project熼      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project殢      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎾      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project斁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project浣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嶇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project疆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project骞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project惰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project烦      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project杩      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project囪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project惤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍚      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project庣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project獥      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectｏ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紱      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project剧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project疆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project椤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project垫      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project柊      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project澧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project炶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瘑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍒      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project姸      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎬      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project佺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project粺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project°      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project8      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project0      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project氫      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project慨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project澶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瀛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project楀      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project箷      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏃      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project犺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project緭      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project洪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project棶      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project棰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project橈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紙      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎾      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project斁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectㄥ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瓧      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project骞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project曞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project紑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍏      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project宠      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project仈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍔      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectㄨ      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瘑      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍒      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project绾      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project裤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎾      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project斁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project涓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project旀      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project縺      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project娲      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project荤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project敤      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鐪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project熷      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project疄      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鎾      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project斁      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鏃      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project堕      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project棿      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project閲      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嶅      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project缓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project娓      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project告      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project爣      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project銆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project佹      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectā      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鍨      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嬪      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project姞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project杞      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project芥      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project湡      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project璇      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嗗      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project埆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project寰      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project幆      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project鑷      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project姩      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project閲      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project嶈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project瘯      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project锛      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project夈      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project€      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project?      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectF      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectw      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project/      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project2      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project>      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project&      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project1      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project;      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectp      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectb      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecth      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectc      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectG      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectM      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecta      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecty      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectr      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectv      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project$      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project{      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectV      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project}      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project(      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectu      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectg      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectI      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectP      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project)      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project"      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project\      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectn      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecto      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectt      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projects      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project-      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectl      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecte      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectR      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectL      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectA      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project_      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectN      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectO      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectT      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectE      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectS      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project.      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectm      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectd      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project       - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projectf      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode projecti      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Fix project.yml encoding
        run: |
          iconv -f UTF-8 -t UTF-8 project.yml 2>/dev/null || cat project.yml > /tmp/project.yml.tmp && mv /tmp/project.yml.tmp project.yml
          echo "project.yml encoding fixed"

      - name: Generate Xcode project
